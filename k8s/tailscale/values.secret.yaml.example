# Copy to values.secret.yaml and fill in OAuth credentials.
# values.secret.yaml is gitignored — never commit it.
#
# Generate credentials at: https://login.tailscale.com/admin/settings/oauth
oauth:
  clientId: ""
  clientSecret: ""
